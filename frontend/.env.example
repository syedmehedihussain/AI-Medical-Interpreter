VITE_API_BASE_URL=http://localhost:8000

# Supabase auth (v0.2 accounts). Leave blank to run guests-only. From the same
# project's Settings > API. The anon key is public/safe for the browser; the
# service_role key must NOT go here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
